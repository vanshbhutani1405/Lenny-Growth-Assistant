# Agent transcript archive

This directory contains concise development-log summaries, not raw model transcripts. They were written from the repository and recorded implementation context so a reviewer can follow the engineering progression without exposing private prompts, credentials, database URLs, or fabricated user conversations.

These summaries are not product evidence. Runtime claims still require the commands and manual checks in `SUBMISSION_CHECKLIST.md` and `manual-testing.md`.
